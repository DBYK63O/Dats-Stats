SELECT DISTINCT q.user_id,
h.user_id IS NOT NULL AS 'is_home_try_on',
h.number_of_pairs,
p.user_id IS NOT NULL AS 'is_purhcase'
FROM quiz AS q
  LEFT join home_try_on AS h
    ON q.user_id = h.user_id
  LEFT join purchase AS p
    ON q.user_id = p.user_id
LIMIT 10;